<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Callibri App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Callibri"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Callibri App"
            android:theme="@style/Theme.Callibri.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


<!--    textView.setOnClickListener {-->
<!--    if (navController.currentDestination?.id != R.id.nav_gallery) {-->
<!--    val navOptions = NavOptions.Builder()-->
<!--    .setPopUpTo(R.id.nav_home, inclusive = true)-->
<!--    .build()-->
<!--    navController.navigate(R.id.nav_gallery, null, navOptions)-->
<!--    drawerLayout.closeDrawer(GravityCompat.START)-->
<!--    } else {-->
<!--    drawerLayout.closeDrawer(GravityCompat.START)-->
<!--    }-->
<!--    }-->

</manifest>